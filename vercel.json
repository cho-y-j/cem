{
  "buildCommand": "vite build && esbuild api/index.ts --bundle --platform=node --format=cjs --outfile=api/index.js --external:@vercel/node --packages=bundle",
  "installCommand": "pnpm install",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}

