# 문서 스캔 보정 기능 추가 계획

## 현재 상태
- `client/src/pages/mobile/DocumentUpload.tsx` - 모바일 서류 업로드 페이지 존재
- 카메라 촬영/파일 선택 기능 구현됨
- 미리보기 기능 있음
- **없는 것**: 모서리 감지, 원근 보정, 색상 보정

## 목표
사진 촬영 후 **스캔 보정 단계 추가**:
1. 모서리 자동 감지 (4개 포인트)
2. 사용자가 모서리 드래그로 조정
3. 원근 보정 (마름모 → 직사각형)
4. 색상 필터 선택 (원본/흑백/문서모드/선명하게)
5. 보정된 이미지로 업로드

---

## 구현 계획

### 1. OpenCV.js 설치
```bash
# CDN 방식 사용 (빌드 크기 최소화)
# public/index.html 또는 동적 로드
```

### 2. 문서 스캔 컴포넌트 생성

#### 파일 구조
```
client/src/
├── components/
│   └── DocumentScanner/
│       ├── index.tsx              # 메인 스캐너 컴포넌트
│       ├── CornerSelector.tsx     # 모서리 드래그 UI
│       ├── ImageFilters.tsx       # 색상 필터 선택
│       └── useOpenCV.ts           # OpenCV 로딩 훅
└── utils/
    └── documentScanner.ts         # OpenCV 로직 (모서리 감지, 원근 변환)
```

### 3. 플로우 변경

**기존:**
```
카메라 촬영 → 미리보기 → 업로드
```

**변경:**
```
카메라 촬영 → [스캔 보정] → 미리보기 → 업로드
              ↓
        1. 모서리 감지/조정
        2. 원근 보정
        3. 색상 필터 선택
        4. 확인
```

---

## 상세 구현

### Phase 1: OpenCV.js 설정
- [ ] OpenCV.js CDN 동적 로드
- [ ] 로딩 상태 관리 (useOpenCV 훅)

### Phase 2: 모서리 감지
- [ ] Canny Edge Detection
- [ ] Contour Detection → 사각형 찾기
- [ ] 4개 포인트 추출
- [ ] 자동 감지 실패 시 수동 선택 모드

### Phase 3: 모서리 조정 UI
- [ ] 캔버스에 이미지 표시
- [ ] 4개 모서리에 드래그 핸들
- [ ] 터치/마우스 드래그 지원
- [ ] 실시간 윤곽선 표시

### Phase 4: 원근 보정
- [ ] getPerspectiveTransform()
- [ ] warpPerspective()
- [ ] 결과 이미지 생성

### Phase 5: 색상 필터
- [ ] 필터 옵션:
  - 원본 (Original)
  - 흑백 (Grayscale)
  - 문서 모드 (고대비 흑백, 배경 제거)
  - 선명하게 (Sharpen + Contrast)
- [ ] 필터 미리보기
- [ ] 밝기/대비 슬라이더 (선택)

### Phase 6: DocumentUpload.tsx 통합
- [ ] 촬영 후 스캐너 모달/화면 열기
- [ ] 보정 완료 후 미리보기에 반영
- [ ] 기존 업로드 로직 유지

---

## UI 디자인

```
┌─────────────────────────────┐
│  ← 문서 스캔                 │
├─────────────────────────────┤
│                             │
│   ┌─────────────────────┐   │
│   │    ○─────────────○  │   │ ← 드래그 가능한 모서리
│   │    │             │  │   │
│   │    │   [문서]    │  │   │
│   │    │             │  │   │
│   │    ○─────────────○  │   │
│   └─────────────────────┘   │
│                             │
├─────────────────────────────┤
│  [원본] [흑백] [문서] [선명] │ ← 필터 선택
├─────────────────────────────┤
│      [ 자동 감지 다시 ]      │
│      [    보정 완료    ]     │
└─────────────────────────────┘
```

---

## 기술 스택

| 기능 | 라이브러리 |
|------|-----------|
| 이미지 처리 | OpenCV.js (CDN) |
| 캔버스 조작 | HTML5 Canvas API |
| 터치 드래그 | React 이벤트 + useRef |

---

## 구현 순서

1. **useOpenCV.ts** - OpenCV 동적 로드 훅
2. **documentScanner.ts** - 모서리 감지 + 원근 변환 유틸
3. **CornerSelector.tsx** - 모서리 드래그 UI
4. **ImageFilters.tsx** - 색상 필터 UI
5. **DocumentScanner/index.tsx** - 메인 컴포넌트
6. **DocumentUpload.tsx 수정** - 스캐너 통합

---

## 예상 코드 크기

- OpenCV.js: ~8MB (WASM) - CDN에서 동적 로드
- 커스텀 코드: ~500줄

---

## 시작할까요?

확인되면 Phase 1부터 구현 시작하겠습니다!
